name: Expire Medicine Reminder

on:
  schedule:
    # Runs DAILY at 9:00 AM and 6:00 PM PKT. The script itself decides
    # (via true epoch-day parity, see send-expiry-reminder.js) whether
    # today is an actual "send" day — giving accurate every-other-day
    # behavior with no month-boundary drift.
    - cron: '0 4 * * *'   # 09:00 AM PKT
    - cron: '0 13 * * *'  # 06:00 PM PKT
  workflow_dispatch:

jobs:
  send-reminder:
    runs-on: ubuntu-latest
    timeout-minutes: 5  # safety net: force-kill if it ever hangs, so it can't burn hours of quota
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Send expiry check reminder (if scheduled today)
        run: node send-expiry-reminder.js
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          FCM_TOPIC: 'all_users'
